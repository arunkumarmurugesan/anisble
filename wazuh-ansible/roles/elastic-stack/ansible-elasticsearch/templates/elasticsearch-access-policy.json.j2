{
	"Version": "2012-10-17",
	"Statement": [{
		"Effect": "Allow",
		"Principal": {
			"AWS": "arn:aws:iam::{{ aws.account_id }}:root"
		},
		"Action": "es:*",
		"Resource": "arn:aws:es:{{ aws.region }}:{{ aws.account_id }}:domain/{{ elasticsearch.domain_name }}/*"
	}]
}
